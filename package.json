{
  "name": "activewatch-system",
  "version": "1.0.0",
  "description": "Unified orchestration for ActiveWatch Portal and Admin Console",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin-dashboard\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin-dashboard"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
